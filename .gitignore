# Compiled output
GUARD.exe
GUARD.pdb

# Shipping artifacts: staged release folder, its zip, and the checksum
# manifest the updater verifies against (publish-release.cmd)
/GUARD/
GUARD.zip
SHA256SUMS

# .NET SDK build output
bin/
obj/

# Generated at runtime by the app
backup-settings.ini
guard-backup.cmd
guard-system-image.cmd
guard-prefs.ini
onconnect-stamp.txt
restore-hold.txt
Logs/

# Personal Claude Code project context
CLAUDE.md
